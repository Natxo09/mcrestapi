meta {
  name: Remove CORS Origin
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/admin/cors?origin=http://localhost:3000
  body: none
  auth: bearer
}

auth:bearer {
  token: {{masterKey}}
}
